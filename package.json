{
  "name": "ranchsamples",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Community-owned, self-hostable audio-samples platform (monorepo root).",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @ranchsamples/web dev",
    "build": "pnpm --filter @ranchsamples/web build",
    "check": "pnpm -r check",
    "lint": "pnpm -r lint"
  }
}
