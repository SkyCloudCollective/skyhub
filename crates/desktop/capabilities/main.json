{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "main",
  "description": "Core capabilities for the RanchSamples desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "drag:default"
  ]
}
