// Static SPA: no server-side rendering, no prerender. The app boots in the
// browser and fetches the API at runtime (see svelte.config.js adapter-static).
export const ssr = false;
export const prerender = false;
