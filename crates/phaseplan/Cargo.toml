[package]
name = "phaseplan"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "PhasePlan — a modular subtractive + wavetable synth with an open modulation matrix (inspired by Phase Plant). Built on dsp-core."

[lib]
crate-type = ["rlib"]

[dependencies]
dsp-core = { workspace = true }
